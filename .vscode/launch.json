{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Python: FastAPI (Estable)",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn", 
            "args": [
                "main:app",
                "--reload",
                "--port", "8001" // 👈 ¡AÑADE ESTAS DOS LÍNEAS!
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}